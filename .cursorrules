---
description: "Git commit message language and format"
alwaysApply: true
---

生成 git commit message 时必须遵循：
- 使用中文。
- 使用格式：`<type>: <简短描述>`。
- `type` 只能使用：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`、`style`、`perf`。
- 不要添加句号、署名、解释说明或 Markdown。
- 示例：
  - `feat: 新增测试用例批量导入功能`
  - `fix: 修复接口测试环境变量替换失败`
  - `docs: 更新 RAGFlow 工作流说明`